-- 025c_drop_exam_code.sql
-- Дата: 2026-09-10
-- Задача: IMTAHAN_KODU_TASK.md — «у экзаменов код нам не нужен, снеси; уникальность по названию
-- и дате поставь» (заказчик, 10.09.2026).
--
-- Зачем. exams.code — самостоятельное трёхзначное пространство номеров, никак не связанное с
-- иерархией кодов район→школа→учитель→ученик (utils/entity-codes.const.ts: teacher =
-- floor(student / 1000) и т.д.) — эта иерархия миграцией не задевается вообще, здесь удаляется
-- только код экзамена. Ни одной внешней ссылки на exams.code в БД нет: student_results.exam_id
-- и booklets.exam_id ссылаются на exams.id, code держал только собственный UNIQUE. Единственный
-- потребитель code как ключа идемпотентности — processExamsFromExcel (импорт списка экзаменов
-- из Excel) — сам метод не смонтирован ни на один роут (exam.routes.ts не имеет upload-эндпоинта),
-- то есть мёртв; ему оставлен минимальный компилируемый вид (дедуп по name+date), новый ключ
-- идемпотентности не изобретался (IMTAHAN_KODU_TASK.md §5).
--
-- Колонка удаляется сразу этой же миграцией, а не откладывается на «колонка рядом, снос
-- отдельно» — в отличие от приёма, которым в 025b_student_fullname.sql обошлись с
-- students.last_name/first_name/middle_name. Там склейка ФИО необратима (двойные фамилии,
-- отсутствующее отчество не восстановить обратно из fullname), здесь же exams.code — порядковый
-- номер без смысла вне UI и без единой ссылки на него откуда-либо ещё; pg_dump перед миграцией
-- (делает apply-pending.sh) — достаточный путь отката (IMTAHAN_KODU_TASK.md §2, решение 3).
--
-- ============================================================================================
-- ГЕЙТ — ЭТУ МИГРАЦИЮ НЕЛЬЗЯ ПРИМЕНЯТЬ, ПОКА ОН НЕ ЗАКРЫТ (по образцу гейта Г1 из
-- 023_exam_types_and_level_scales.sql — там для его закрытия хватило рассуждения без данных;
-- здесь по-другому: нужен факт из живых данных прода).
--
-- UNIQUE (name, date) ниже упадёт прямо на этой строке ALTER TABLE, если в проде уже есть два
-- экзамена с одинаковым названием и датой. Перед применением на проде обязательно прогнать:
--
--     SELECT name, date, count(*) FROM exams GROUP BY name, date HAVING count(*) > 1;
--
-- Пусто — применяем как есть. Не пусто — заказчик решает, что делать с дублями (переименовать/
-- удалить/что-то ещё), эту миграцию до его решения НЕ ПРИМЕНЯТЬ. Запрос отправлен заказчику
-- 10.09.2026, ответа на момент написания миграции нет (IMTAHAN_KODU_TASK.md §3).
--
-- Результат прогона гейта на локальном одноразовом Postgres (не прод, синтетические данные —
-- см. отчёт по задаче): вписан в IMTAHAN_KODU_TASK.md §3. Прод этим прогоном не проверялся и
-- не может быть — эта миграция прод не трогает.
-- ============================================================================================

BEGIN;

ALTER TABLE exams DROP COLUMN code;
ALTER TABLE exams ADD CONSTRAINT exams_name_date_key UNIQUE (name, date);

COMMIT;
