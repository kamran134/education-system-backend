/**
 * Заказчик, 25.08.2026 (BASE_FIXES_TASK.md §4.5): «İnkişaf edən şagirdlər» на плитке профиля
 * считается только по тем, кто участвовал минимум 3 раза за учебный год — чтобы разовый
 * всплеск (пришёл один раз в апреле, «вырос») не завышал процент. Захардкожено намеренно —
 * не выносим в настройки админки, заказчик уже дважды менял такие цифры, но каждый раз это
 * была одна строка кода, а не конфигурируемая настройка.
 *
 * Не путать с селектом «İştirak sayı (minimum)» на /statistics — та цифра остаётся
 * пользовательской (по умолчанию 2 у админов, 3 у остальных ролей), решение владельца
 * 25.08.2026 её не трогать.
 */
export const MIN_PARTICIPATIONS_FOR_DEVELOPMENT = 3;
